default:
  image: node:16.18.0-alpine
  tags:
    - "tokend"

stages:
  - deploy

pages:
  stage: deploy
  script:
    - yarn install
    - yarn typecheck
    - yarn build
    - mv ./build ./public
  artifacts:
    paths:
      - public
  only:
    - main
